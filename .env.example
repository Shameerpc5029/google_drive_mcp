# Google Drive MCP Server Configuration

# Path where credentials will be stored (REQUIRED)
GDRIVE_CREDENTIALS_PATH=./.gdrive-server-credentials.json

# Google OAuth Client Credentials (get these from Google Cloud Console)
# Create OAuth2 credentials at: https://console.cloud.google.com/apis/credentials
GDRIVE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GDRIVE_CLIENT_SECRET=your-client-secret-here

# OAuth Tokens (these will be generated after running auth flow)
# Leave these empty initially - they will be filled after running: npm run auth
GDRIVE_ACCESS_TOKEN=
GDRIVE_REFRESH_TOKEN=

# Optional: Server configuration
PORT=3000
MCP_LOG_LEVEL=info

# Optional: Transport mode for Docker (stdio|websocket|sse)
MCP_TRANSPORT=sse
